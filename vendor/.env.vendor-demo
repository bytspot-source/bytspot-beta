# Loaded only by --mode vendor-demo, so the default vendor build never sees it.
# The demo transport accepts any well-formed code, which is why this flag must
# never be set on a build that is served from vendor.bytspot.app.
VITE_VENDOR_DEMO=true
